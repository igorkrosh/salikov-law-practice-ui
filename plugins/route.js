export default function ({ app }) {
    /*
    app.router.beforeEach((to, from, next) => {
        app.store.dispatch('SET_LOADING', true)
        console.log('route')
        setTimeout(next, 500);
    })
    */
}