import Vue from 'vue';

Vue.mixin({
    /*
    methods: {
        LoadingDone()
        {
            this.$store.dispatch('SET_LOADING', false);
        },
        LoadingStart() {
            //this.$store.dispatch('SET_LOADING', true);
            this.$nuxt.$loading.start()
        }
    }
    */
})